<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Products</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style type="text/css">
        body {
            margin-top: 20px;
            background: #eee;
        }
        .ui-w-40 {
            width: 40px !important;
            height: auto;
        }

        .card {
            box-shadow: 0 1px 15px 1px rgba(52, 40, 104, .08);
        }

        .ui-product-color {
            display: inline-block;
            overflow: hidden;
            margin: 0.144em;
            width: 0.875rem;
            height: 0.875rem;
            border-radius: 10rem;
            -webkit-box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.15) inset;
            box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.15) inset;
            vertical-align: middle;
        }
    </style>
</head>
<body>
<div class="container px-3 my-5 clearfix">
    <div class="card">
        <div class="card-header">
            <h2>Products</h2>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered">
                    <thead>
                    <tr>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Price</th>
                        <th>Date Fabrication</th>
                        <th>Quantity</th>
                        <th>Likes</th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in products %}
                        <tr>
                            <td><img src="{{ asset('product_images/' ~ item.image) }}" class="ui-w-40" alt=""></td>
                            <td>{{ item.name }}</td>
                            <td>{{ item.price }}</td>
                            <td>{{ item.datefabrication|date('Y-m-d') }}</td>
                            <td>{{ item.quantite }}</td>
                            <td>{{ item.likes }}</td>
                            <td>
                                <a href="{{ path('product_edit', {'id': item.id}) }}" class="btn btn-primary btn-sm">Edit</a>
                                <form method="post" action="{{ path('product_delete', {'id': item.id}) }}" class="d-inline">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                </form>
                                 <a href="{{ path('generate_pdf', {'id': item.id}) }}" class="btn btn-success btn-sm">
                                    PDF
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
                <a href="{{ path('product_new') }}" class="btn btn-primary btn-sm">Create a new product</a>
                <a href="{{ path('generate_excel') }}" class="btn btn-success btn-sm">
    <i class="fa fa-file-excel-o"></i> Generate Excel
</a>
            </div>
        </div>
    </div>
</div>
<script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.1.1/dist/js/bootstrap.bundle.min.js"></script>
<script type="text/javascript"></script>
</body>
</html>
